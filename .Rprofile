if (requireNamespace("renv", quietly = TRUE)) {
  renv::activate()
}

if (interactive() && requireNamespace("devtools", quietly = TRUE)) {
  message("Run devtools::load_all() to load the package for development.")
}
